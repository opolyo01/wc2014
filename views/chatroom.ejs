<% include header.ejs %>

<% if(user) { %>
	<%- include navbar %>
	<%- include chat %>
<% } 

else {%>
	<% 	var message; 
		if(message) { %>
		<div class="error"><%= message %></div>
	<% } %>
	<%- include login %>
<% } %>
